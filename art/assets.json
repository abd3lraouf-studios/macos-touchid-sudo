{
  "schemaVersion": 3,
  "$schema": "https://abd3lraouf.dev/schema/product-assets.schema.json",
  "schema": {
    "schemaVersion": "Major shape of this file. 3 adds $schema, schema (this guide) and downloads; 2 added theme and marketing; 1 is the bare product record.",
    "$schema": "The published JSON Schema for this file, so editors validate the kit as you type.",
    "schema": "This block: every field of assets.json explained. Structure comes from lib/kit-doc.ts in the site repo — edit structure there, prose here.",
    "product.slug": "URL-safe identifier; the site route is /work/<slug>/ and the press kit /press/<slug>/.",
    "product.name": "Display name, spelled exactly as press.naming requires.",
    "product.category": "\"app\" or \"tool\" — sorts the card into the two landing groups.",
    "product.headline.lead": "First half of the headline duet.",
    "product.headline.lift": "The italic-serif half of the headline. Required.",
    "product.headline.trail": "Optional closing half.",
    "product.tagline": "One line under the headline on the card.",
    "product.summary": "Longer one-liner for the product page hero and press kit.",
    "product.platform": "Long-form platform string for the fact rows.",
    "product.licence.label": "How the licence reads on the card.",
    "product.licence.kind": "oss | free | paid — drives the label ink and JSON-LD offers.",
    "product.licence.price": "Price line for the fact rows.",
    "product.licence.pricePoint": "For kind \"paid\": the fragment that costs money, e.g. \"$5 Pro\".",
    "product.distribution": "Where it is installed from, as a phrase.",
    "product.openSource": "true = MIT (the footer counts these).",
    "product.mono": "Fallback monogram for an icon-less kit; null when an icon exists.",
    "links.repo": "The repository's real https:// URL — the fetch script resolves it directly.",
    "links.store": "Optional store listing (Mac App Store). Shown as a fact row and the hero fallback badge.",
    "links.website": "Optional product/site URL.",
    "press.short": "One-paragraph boilerplate for the press kit.",
    "press.long": "Full boilerplate; also closes the product page facts.",
    "press.naming": "How to spell the product name in print.",
    "theme.accent": "v2+. Per-app accent as a bare HSL triplet (\"H S% L%\"), contrast-gated at validation.",
    "marketing.sections": "v2+. Ordered typed sections the product page renders through one renderer; kinds: features, steps, stats, showcase, table, faq, prose, terminal. Each may carry an optional \"lift\" beside its heading — the italic-serif half of the duet, one per heading, on the line carrying the promise. The page alternates its own grounds across this run; a kit chooses what to say, never where the bands fall.",
    "downloads": "v3+. 1-4 real ways to get the product. The FIRST entry must be url-based — it renders as the hero primary button. Absent = the hero falls back to the repository link.",
    "downloads[].label": "Button or chip text.",
    "downloads[].kind": "appstore | releases | dmg | zip | exe | appimage | website | repo (url-based) or brew | script (command-based).",
    "downloads[].url": "https:// URL, required for url-based kinds.",
    "downloads[].command": "Copyable install command, required for brew/script.",
    "downloads[].note": "Optional caveat shown near the CTA.",
    "assets": "Every art file, flat basenames only, with role, label, note and MEASURED width/height (fetch re-measures and throws on mismatch).",
    "assets[].role": "icon-card | icon-press | icon-vector | screenshot | screenshot-localized | lockup | hero | diagram | benchmark | terminal. icon-* / screenshot / lockup are singletons; screenshot-localized may repeat, once per language.",
    "assets[].file": "Flat basename sitting next to this assets.json.",
    "assets[].label": "Caption shown in the press kit.",
    "assets[].note": "Format note on the download badge, e.g. \"PNG\".",
    "assets[].width": "Measured intrinsic width in px.",
    "assets[].height": "Measured intrinsic height in px."
  },
  "product": {
    "slug": "touch-id-for-sudo",
    "name": "Touch ID for sudo",
    "category": "tool",
    "headline": {
      "lead": "Authenticate",
      "lift": "sudo with Touch ID"
    },
    "tagline": "including inside tmux",
    "summary": "Enables Touch ID authentication for sudo, and keeps it working inside tmux.",
    "platform": "macOS 14+",
    "licence": {
      "label": "MIT",
      "kind": "oss",
      "price": "Free"
    },
    "distribution": "GitHub releases, source on GitHub",
    "openSource": true,
    "mono": null
  },
  "links": {
    "repo": "https://github.com/abd3lraouf-studios/macos-touchid-sudo",
    "website": "https://abd3lraouf.dev/work/touch-id-for-sudo/"
  },
  "press": {
    "short": "Touch ID for sudo enables fingerprint authentication for sudo on macOS 14+, including inside tmux. MIT licensed.",
    "long": "macOS can authenticate sudo with Touch ID, but the configuration does not survive system updates and breaks inside tmux. Touch ID for sudo sets it up correctly, keeps it working across updates, and handles the tmux case that the stock configuration misses. MIT licensed and open source.",
    "naming": "Written \"Touch ID for sudo\" — \"Touch ID\" capitalised as Apple styles it, \"sudo\" always lowercase. Touch ID is an Apple trademark and the project is unaffiliated."
  },
  "theme": {
    "accent": "347 74% 47%"
  },
  "marketing": {
    "sections": [
      {
        "kind": "steps",
        "heading": "Two commands, once",
        "lift": "then never again.",
        "intro": "The stock configuration is one line in the wrong file — this puts it in the right one.",
        "steps": [
          {
            "title": "brew install",
            "body": "The formula arrives from the studio's tap, with pam-reattach alongside it for the tmux case."
          },
          {
            "title": "sudo touchid-sudo",
            "body": "Writes the pam_tid.so line into sudo_local, the file macOS 14+ created for exactly this kind of local override. Unrelated lines are preserved."
          },
          {
            "title": "Then just use sudo",
            "body": "A fingerprint instead of a password — in a terminal, in an IDE, and inside tmux, which is the case the stock setup always missed."
          }
        ]
      },
      {
        "kind": "table",
        "heading": "What the script actually does",
        "lift": "in the order it does it.",
        "columns": [
          "Stage",
          "What happens"
        ],
        "rows": [
          [
            "Preflight",
            "macOS, Sonoma-or-later include line, pam_tid.so present, fingerprint enrolled"
          ],
          [
            "Backup",
            "previous config saved to /etc/pam.d/sudo_local.bak"
          ],
          [
            "Validation",
            "runs sudo -n as you afterwards and inspects the result for PAM errors"
          ],
          [
            "Rollback",
            "restores the backup automatically if validation fails, and exits non-zero"
          ],
          [
            "Idempotent",
            "re-running changes nothing; unrelated lines in sudo_local are preserved"
          ]
        ],
        "mono": [
          1
        ]
      },
      {
        "kind": "showcase",
        "asset": "touchid-prompt.png",
        "caption": "The prompt, inside tmux — the case the stock configuration misses."
      },
      {
        "kind": "terminal",
        "command": "brew install abd3lraouf-studios/tap/macos-touchid-sudo\nsudo touchid-sudo",
        "note": "Homebrew is the recommended path; a curl installer is in the README."
      },
      {
        "kind": "prose",
        "heading": "The security answer",
        "lift": "one fewer keylogger surface.",
        "body": "sudo still requires local presence and a biometric match, and falls back to your password rather than around it. The meaningful change is that a sudo prompt no longer traverses your keyboard, which removes one keylogger surface. Anyone who can already run code as you is not stopped by either mechanism.\n\nThe install script is short and readable, and it is the thing running as root: read it before you run it. Preflight, backup, validation and rollback are all in the table above, not in the marketing copy."
      },
      {
        "kind": "faq",
        "heading": "Questions",
        "items": [
          {
            "q": "Does it survive macOS updates?",
            "a": "Yes. sudo_local is the file Apple ships for local PAM overrides, and system updates leave it alone. That is the whole reason the line goes there and not into sudo's own file."
          },
          {
            "q": "How do I undo it?",
            "a": "sudo touchid-sudo --disable reverts to password-only sudo, then brew uninstall removes the tool."
          },
          {
            "q": "Why doesn't the stock setup cover tmux?",
            "a": "tmux runs sudo in a server process outside your terminal session, so the Touch ID prompt never reaches it. pam-reattach bridges that, and the installer wires it in when it is present."
          },
          {
            "q": "Is the installer safe to run?",
            "a": "It runs as root, so read it first — it is one short script. It preflights, backs up sudo_local, validates with sudo -n, and rolls back on any failure."
          }
        ]
      }
    ]
  },
  "downloads": [
    {
      "label": "Get it on GitHub",
      "kind": "repo",
      "url": "https://github.com/abd3lraouf-studios/macos-touchid-sudo"
    },
    {
      "label": "Homebrew",
      "kind": "brew",
      "command": "brew install abd3lraouf-studios/tap/macos-touchid-sudo"
    }
  ],
  "assets": [
    {
      "role": "icon-card",
      "file": "touch-id-for-sudo-icon.png",
      "label": "App icon, 256px",
      "note": "PNG",
      "width": 256,
      "height": 256
    },
    {
      "role": "icon-vector",
      "file": "touch-id-for-sudo-icon.svg",
      "label": "App icon, vector",
      "note": "SVG",
      "width": 512,
      "height": 512
    },
    {
      "role": "icon-press",
      "file": "touch-id-for-sudo-icon-512.png",
      "label": "App icon, 512px",
      "note": "PNG",
      "width": 512,
      "height": 512
    },
    {
      "role": "screenshot",
      "file": "touchid-prompt.png",
      "label": "Touch ID prompt",
      "note": "PNG",
      "width": 548,
      "height": 626
    }
  ]
}
